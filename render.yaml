services:
  - type: web
    name: truthlens-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt && cd backend/model && python multi_model.py"
    startCommand: "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0